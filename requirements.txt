# Vercel Python runtime installs these (uvicorn NOT needed — Vercel provides the ASGI server).
fastapi>=0.110
python-multipart>=0.0.9
geographiclib>=2.0
psycopg[binary]>=3.1
PyJWT[crypto]>=2.8   # [crypto] pulls `cryptography` — required to verify ES256 (JWKS) tokens
httpx>=0.27
